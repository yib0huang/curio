# 安全策略

## 当前支持范围

Curio 目前处于 `0.x` 本地原型阶段，尚未提供面向最终用户的稳定版本支持承诺。安全修复会优先进入最新代码。

## 报告安全问题

请不要在公开 Issue 中披露可利用的漏洞、密钥或用户数据。请通过仓库维护者提供的私密联系方式报告，并包含：

- 受影响版本或提交
- 问题描述和潜在影响
- 最小复现步骤
- 建议修复方式（如有）

远端仓库建立后，应在此处补充明确的私密安全报告渠道。

## 安全基线

- 网页正文和模型输出均按不可信输入处理。
- UI 不通过 `innerHTML` 渲染网页或模型返回内容。
- Manifest V3 中不执行远程代码。
- 密钥不会写入仓库、日志或发布包。
- 新增 Chrome 权限必须遵循最小权限原则并记录用途。
- 正式发布不得将服务端 API Key 随扩展分发；应使用受控后端、用户鉴权、限流和审计。

更多数据处理说明见 `docs/PRIVACY.md`。

